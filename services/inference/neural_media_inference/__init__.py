"""Neural Media inference package — SCAFFOLD ONLY.

This package is owned by the **ml-inference** worker. See
`docs/worker-briefs/ml-inference.md` for what to build and which
interfaces to preserve.

The integration lead has populated this directory with empty module
stubs. Do not move files between modules without coordinating; the API
service imports by these names.
"""

__version__ = "0.0.1-scaffold"
